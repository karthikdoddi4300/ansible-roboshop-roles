- name: copying {{ component }} service
# tempalte modukle
  ansible.builtin.template:
    src: "{{ component }}.service.j2"
    dest: /etc/systemd/system/{{ component }}.service

- name : damon reload after changes
  ansible.builtin.systemd_service:
    daemon_reload: true